/*Добавьте в этот файл пояснения к выбранной модели данных. 
Также вы можете сохранить здесь другие комментарии по проекту, которые вы считаете необходимыми, 
и задать вопросы ревьюеру*/
1. makes: Содержит информацию о производителях автомобилей (марках).
- id: уникальный идентификатор производителя (первичный ключ).
- name: название производителя (уникальное значение). /* Выбрал VARCHAR(255), это текст и тут строки переменной длины*/

2. car_models:Содержит информацию о моделях автомобилей.
- id: уникальный идентификатор модели (первичный ключ).
- name: название модели (уникальное значение). /* Выбрал VARCHAR(255), это текст и тут строки переменной длины, хотя можно было взять и меньше наверно*/
- make_id: идентификатор производителя (внешний ключ, ссылается на makes(id)).

3. colors: Содержит информацию о доступных цветах для автомобилей.
- id: уникальный идентификатор цвета (первичный ключ).
- color: название цвета (уникальное значение). /* Выбрал VARCHAR(255), это текст и тут строки переменной длины, надо было поставить не 255, а 10?*/

4. cars: Содержит информацию о конкретных автомобилях.
- id: уникальный идентификатор автомобиля (первичный ключ).
- model_id: идентификатор модели (внешний ключ, ссылается на car_models(id)).
- gasoline_consumption: потребление топлива автомобилем в литрах. /* Выбрал DECIMAL(5, 2), числа с заданной точностью и точность важна*/
- brand_origin: страна происхождения автомобиля. /* Выбрал VARCHAR(255), это текст и тут строки переменной длины*/
- color_id: идентификатор цвета (внешний ключ, ссылается на colors(id)).

5. customers: Содержит информацию о покупателях.
- id: уникальный идентификатор покупателя (первичный ключ).
- person_name: имя покупателя. /* Выбрал VARCHAR(255), это текст и тут строки переменной длины*/
- phone: номер телефона покупателя. /* Выбрал VARCHAR(255), это текст и тут строки переменной длины*/

6. sales: Содержит информацию о продажах автомобилей.
- id: уникальный идентификатор продажи (первичный ключ).
- car_id: идентификатор автомобиля, который был продан (внешний ключ, ссылается на cars(id)).
- customer_id: идентификатор покупателя (внешний ключ, ссылается на customers(id)).
- sale_date: дата продажи. /* Выбрал Date, так как нудна была дата без времени*/
- price: цена продажи. /* Выбрал DECIMAL(10, 2), числа с заданной точностью и точность важна*/
- discount: скидка на продажу. /* Выбрал DECIMAL(5, 2), числа с заданной точностью и точность важна, ну и мало ли скидку дадут не целое число, а 13.88%*/

Связь один ко многим:
- Один производитель может иметь множество моделей (отношение между makes и car_models).
- Один цвет можно использовать для множества автомобилей (отношение между colors и cars).
- Одна модель может быть представлена множеством автомобилей (отношение между car_models и cars).

Связь многие ко многим:
- один покупатель может купить много автомобилей, и один автомобиль может быть продан многим покупателям.

Пояснения:
В файле INITIAL.sql написан запрос на копирования данных из csv, но у меня выдавал ошибку и я просто данные взял и закинул через интерфейс DBeaver

Вопросы:
1) При импорте csv файла в таблицу базы данных через psql и команду \copy возникает такая ошибка 
ERROR: character with byte sequence 0x98 in encoding "WIN1251" has no equivalent in encoding "UTF8" Это критично?(Разобраться не смог)Если можно через интерфейс импортировать.
2) Sql запросы некоторые писал без использования форматера (sqlformatter) Это критично при оформлении, если у меня скобки не там закрываются?(не с теми отступами) Да и сами отступы, прям 2 пробела нужно? тапам нельзя?
3) И вопрос по заданию, я вначале при составления таблиц везде прописал NOT NULL, чтоб не было пустых значений и не смог загрузить данные, пришлось убрать NOT NULL, как вообще нужно поступать? Не загружать такие данные? Редактировать и ставить какие то значения поумолчанию? Или игнорировать и пропускать? Потому что в задание есть записи с null, а что с ними делать не понял и пропустил их.
